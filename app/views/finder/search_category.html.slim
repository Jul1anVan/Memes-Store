h1
  normal Search:
  = @search_params
h1
  normal Category:
  = @category.name

- if @products.present?
  = render @products, line_item: @line_item
- else
  br
  .lead
    .text-center
      p
        Strong No results found. Please try again.
